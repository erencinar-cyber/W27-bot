name: W27 Yurt Botu

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    # Bota dosyayı güncelleyebilmesi için yazma izni veriyoruz
    permissions:
      contents: write 

    steps:
      - name: Kodlari Getir
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Gerekli Kutuphaneleri Kur
        run: pip install requests

      - name: Botu Calistir
        run: python w27_bot.py

      - name: Hafizayi Kaydet
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          
          # Dosya henüz yoksa (ilk çalışma) hata vermemesi için boş bir tane oluşturuyoruz
          touch hafiza.txt
          
          # Dosyayı ekle
          git add hafiza.txt
          
          # Sadece değişiklik varsa kaydet (yoksa hata vermesini engeller)
          git diff-index --quiet HEAD || git commit -m "Hafiza guncellendi"
          
          # Değişiklikleri GitHub'a yolla
          git push
